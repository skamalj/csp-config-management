apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx-breakglass2
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
